describe('vyper in ts environment', () => {
  it('should run vyper commands', async () => {
    expect(non_ui5).toBeTruthy();
    expect(ui5).toBeTruthy();
    expect(utilities).toBeTruthy();
  });
});

